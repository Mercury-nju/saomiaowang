//
//  ContentView.swift
//  合同扫描王-快速读懂合同&识别风险
//
//  Created by Mercury on 2026/1/9.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
